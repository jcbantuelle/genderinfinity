<% if @search_form.errors.count > 0 %>
  <p>There were errors in your search. Please correct them and try again.</p>
  <ul>
    <% @search_form.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
<%= form_for @search_form, {url: search_index_path, method: :get} do |f| %>
    <section class="primary-content">
        <div class="wrapper">
            <!-- Search Form -->
            <div class="form-group">
                <span class="location-address">
                    <%= f.text_field :address, placeholder: 'Address', tabindex: 5 %>
                    <em class="icon-location"></em>
                </span>
                <span class="mile-radius">
                    <%= f.text_field :search_radius, placeholder: 'Mile Radius', tabindex: 10 %>
                    <em class="icon-radius"></em>
                </span>
                <span class="search">
                    <%= submit_tag 'Search', class: 'btn', tabindex: 15 %>
                </span>
            </div>

            <!-- General Topics -->
            <h2 class="alt"><span>Choose a Service</span></h2>
            <div id="services" class="general-topic">
                <% @services.each do |service| %>
                    <a class="service-link <%= 'active' if service.id.to_s == @search_form.service %>" data-service-value="<%= service.id %>" href="#"><%= service.name %></a>
                <% end %>
                <%= f.hidden_field :service %>
            </div>
        </div>
    </section>

    <!-- Specific Specialties -->
    <section class="specific-specialty">
        <div class="menu" style="<%= 'display:none;' unless @service %>">
            <div class="wrapper">
                <h2>Looking for something more specific? Narrow your search within <strong id="specialty-filter-service-name"><%= @service.name if @service %></strong></h2>
                <div id="specialties" class="column-wrap">
                    <%= render partial: 'specialties' if @service %>
                </div>
            </div>
        </div>
    </section>
<% end %>

<script type="text/javascript">
    $('.service-link').click(function() {
        $('.service-link').removeClass('active')
        $(this).addClass('active')
        $('.menu').slideUp()
        var service_text = $(this).text()
        var service_id = $(this).attr('data-service-value')
        $('#specialty-filter-service-name').text(service_text)
        $('#search_form_service').val(service_id)
        $.post('/search/specialties', {service_id: service_id}, function(specialties) {
            $('#specialties').html(specialties)
            $('.menu').slideDown()
        })
    })
</script>
